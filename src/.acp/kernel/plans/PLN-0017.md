# PLAN: PLN-0017

## Meta
- Related Request: REQ-0018
- Summary: Harden workbench partition rendering geometry to prevent NSBezierPath draw exceptions
- Primary Capability: visual_editor
- Target Slice: editor_canvas
- Affected Modules:
  - tools/window-manager/Sources/WindowManager/UI/DesktopWorkbenchWindow.swift
  - src/.acp/kernel/requests/REQ-0018.yaml
  - src/.acp/kernel/changes/CHG-0017.yaml
- User Confirmation: true
- Created At: 2026-04-25T17:12:25Z
- Status: done

## Goal and Boundary
- Original Need: Workbench crashes with EXC_BREAKPOINT while drawing tabbed zone sections.
- In Scope:
  - sanitize all rounded-rect drawing inputs in DesktopContainerView
  - clamp split and section geometry to finite, non-negative dimensions
  - keep existing interaction and visual behavior
- Out of Scope:
  - redesigning layout modes or stack semantics

## Execution Strategy
- Add geometry guard helpers (finite checks, min size, clamped rect).
- Use safe rects before every NSBezierPath rounded-rect call in DesktopContainerView.
- Clamp split updates from external and drag events to valid finite values.
- Sanitize section frames in buildSections to avoid negative/NaN dimensions.
- Build/test to validate no regressions.

## Validation and Risk
- Acceptance Criteria:
  - no crash when opening/refreshing workbench or dragging split dividers.
  - draw path degrades gracefully for tiny sections.
  - `swift build` and `swift test` pass.
- Risk:
  - tiny regions may skip some visual detail when below drawing threshold.
